<h1>Create a new Figure:</h1>

<form action='/figures' method="POST">
    Figure Name: <input type="text" name="figure[name]" id="figure_name"<br><br>
    
    Choose a Title below or create a new Title:<br>
    <%Title.all.each do |title|%>
    <input type="checkbox" name="figure[title_ids][]" id="title_<%=title.id%>" value="<%=title.id%>"><%=title.name%><br>
    <%end%><br>
    New Title Name: <input type="text" name="title[name]" id="new_title"><br><br>

    Choose a Landmark below or create a new Landmark:<br>
    <%Landmark.all.each do |landmark|%>
    <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%=landmark.id%>" value="<%=landmark.id%>"><%=landmark.name%><br>
    <%end%><br>
    New Landmark Name: <input type="text" name="landmark[name]" id="new_landmark"><br>
    Year Completed: <input type="text" name="landmark[year_completed]" id="new_year_completed">

    <br><br><input type="submit" value="Create New Figure" id="submit">
</form>